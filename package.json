{
  "name": "warplet-gobbler",
  "version": "0.1.0",
  "private": true,
  "description": "WarpletGobbler — PunkStrategy-style flywheel for Warplets using Superfluid streaming",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean",
    "indexer:codegen": "pnpm --filter warplet-activity-indexer codegen",
    "indexer:dev": "pnpm --filter warplet-activity-indexer dev",
    "indexer:start": "pnpm --filter warplet-activity-indexer start",
    "indexer:typecheck": "pnpm --filter warplet-activity-indexer typecheck"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}
